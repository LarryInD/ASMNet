# ASMNet
ASMNet: Axis-decoupled sparse mining for efficient remote sensing object detection

ASMNet.py can be added as a backbone network to, for example, the PKINet repository for training. Subsequently, we will upload the pretrained model weights.
